import { styled } from '@/generated/styled-system/jsx'

export const Blockquote = styled('blockquote', {
	base: {
		backgroundColor: 'bg1',
		borderColor: 'bg2',
		borderLeftWidth: '4px',
		fontSize: 'small',
		pl: 'calc(1rem - 4px)',
	},
})
